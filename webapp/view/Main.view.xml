<mvc:View
	controllerName="pixiedust.controller.Main"
	displayBlock="true"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:core="sap.ui.core"
	core:require="{
		formatter: 'pixiedust/model/formatter'
	}">

	<Page
		title="{i18n>appTitle}"
		id="page">
		<content>
			<IllustratedMessage
				title="{i18n>appTitle}"
				illustrationType="sapIllus-SuccessHighFive"
				enableVerticalResponsiveness="true"
				description="{i18n>appDescription}">
				<additionalContent>
					<Button
						id="helloButton"
						text="{formatter: 'formatter.formatValue', path: 'i18n>btnText'}"
						press="sayHello"/>


					<!-- Button to open the dialog -->
					<Button
						text="New Order"
						press=".onOpenDialog"
					/>

					<!-- Table -->
					<Table id="myTable" items="{/users}">
						<columns>
							<Column>
								<Text text="Name"/>
							</Column>
							<Column>
								<Text text="Link"/>
							</Column>
							<Column>
								<Text text="Quantity"/>
							</Column>
							<Column>
								<Text text="Price"/>
							</Column>
							<Column>
								<Text text="Notes"/>
							</Column>
						</columns>
						<items>
							<ColumnListItem>
								<cells>
									<Text text="{name}"/>
									<Text text="{link}"/>
									<Text text="{quantity}"/>
									<Text text="{price}"/>
									<Text text="{notes}"/>
								</cells>
							</ColumnListItem>
						</items>
					</Table>

					<!-- Dialog Definition -->
					<Dialog id="userDialog" title="New Order" afterClose="onDialogClose">
						<VBox>
							<Label text="Name"/>
							<Input id="nameInput"/>
							<Label text="Link"/>
							<Input id="link"/>
							<Label text="Quantity"/>
							<Input id="quantity"/>
							<Label text="Price"/>
							<Input id="price"/>
							<Label text="Notes"/>
							<Input id="notes"/>
						</VBox>
						<beginButton>
							<Button text="Add" press="onAddUser"/>
						</beginButton>
						<endButton>
							<Button text="Cancel" press="onCancel"/>
						</endButton>
					</Dialog>
				</additionalContent>
			</IllustratedMessage>
		</content>
	</Page>

</mvc:View>
